var props = require('./op');
var emiter = require('./emitter');
var inheritFromEvent = require('./inheritance');

module.exports = {
    props:props,
    emiter:emiter,
    inheritous:inheritFromEvent
}